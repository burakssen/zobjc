//! Objective-C instance variable (Ivar) handle.
//!
//! Typed wrapper around runtime Ivar entities.
//! Implemented in Phase 2.

const raw = @import("../raw/root.zig");

// TODO(phase-2): Implement full Ivar handle, offset, and type encoding reflection.
pub const Ivar = opaque {};
